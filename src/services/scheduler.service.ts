// No longer used after deployment.
// Scheduler execution now lives in `src/jobs/scheduler.job.ts`
// and is triggered either by local app startup or the deployed cron route.

// import { startSchedulerJob } from '../jobs/scheduler.job';

// export const schedulerService = {
//   start() {
//     startSchedulerJob();
//   }
// };
