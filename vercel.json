{
  "version": 2,
  "buildCommand": "prisma generate",
  "builds": [
    { "src": "src/app.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/app.ts" }
  ],
  "crons": [
    {
      "path": "/api/internal/scheduler/tick",
      "schedule": "*/5 * * * *"
    }
  ]
}
